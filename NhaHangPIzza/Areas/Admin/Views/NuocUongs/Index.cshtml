@model IEnumerable<NhaHangPIzza.Models.NuocUong>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenNuocUong)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GiaTien)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenNuocUong)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GiaTien)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaNuocUong }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaNuocUong }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaNuocUong })
        </td>
    </tr>
}

</table>
